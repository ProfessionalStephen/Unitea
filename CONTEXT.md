# Claude Browser Agent -- Session Context

## Role
Drives Stephen's signed-in browser. Verifies deployed features.
Ships code inline via PowerShell writes each session.

## Repo
@'
# Unicity KPI Briefing System — Project Context

> Auto-generated by setup-skills. Keep updated as the project evolves.

## What this is
Morning KPI briefing system for Unicity Solar Energy. Vercel-deployed Vite
React SPA + serverless API that:
- Pulls live pipeline data from Pipedrive at 6am ET weekdays
- Generates personalised HTML briefing emails for up to 30 team members
- Dashboard at https://unicity-kpi.vercel.app

## Key constraints
- No GitHub auth for Claude -- code ships inline via PowerShell writes each session.
- Repo lives at C:\UnicityDev\Unitea
- Windows PowerShell. Absolute paths. Never ../
- Vercel free tier -- serverless functions, Vercel Blob, no DB.
- Stephen is non-technical. Trust the tooling.

## Stack
| Layer | Tech |
|---|---|
| Frontend | React 19, TypeScript, Vite 8 |
| Backend | Vercel serverless (@vercel/node) |
| Email | Gmail API via OAuth2 |
| Pipeline data | Pipedrive REST API |
| Persistence | Vercel Blob (kpi-tags config, daily snapshots) |
| Auth | Google OAuth (sign-in), CRON_SECRET (cron auth) |
| CI | GitHub Actions (tsc + vitest on PRs) |
| Tests | Vitest, 136+ tests |

## Repo layout
api/
  _lib/           # pipedrive.ts, kpi-config-store.ts, snapshot.ts
  cron/
    send-briefings.ts   # CRITICAL -- main cron handler
    status.ts
  email/ pipedrive/ snapshots/ auth/ config/
shared/
  domain/
    team.ts        # TEAM_INIT -- 30 members, emailedTeam()
    roles.ts       # boardsForRole(), kpisForRole()
    boards.ts, kpi-configs.ts, types.ts
  history/ kpi/
src/               # React frontend (App.tsx ~1700 lines -- split queued)
tests/
docs/
  adr/             # ADR-001, ADR-002
  agents/
.github/workflows/ci.yml

## Deployed environment (Vercel)
EMAILS_PAUSED        -- true = block all sends
CRON_TEST_RECIPIENT  -- if set, cron sends ONLY to this address
CRON_SECRET          -- bearer token for cron auth
PIPEDRIVE_API_KEY / PIPEDRIVE_DOMAIN
GOOGLE_CLIENT_ID / GOOGLE_CLIENT_SECRET
SENDER_REFRESH_TOKEN / SENDER_EMAIL

## Known: aparis@unicitysolar.com appears twice
id 6 (Warehouse Manager) and id 28 (AI Engineer). Both roles intentionally
kept. Deduped at email-send time in send-briefings.ts.

## Current state (2026-05-15)
- Live mode active, test recipient pinned to Stephen Farrell
- EMAILS_PAUSED=true safety net still set
- 0 snapshots -- first writes Mon 2026-05-18 cron run
- KPI cards N/A -- KPI Mapping has no data sources configured
- RALPH tab: 0 open issues, clean

## Queued work
1. [done] aparis dedup, pin @vercel/node, CI workflow
2. [done] ADR-001 live-mode boundary
3. [done] ADR-002 RALPH automated loop design
4. App.tsx split (~3hr, before new feature UI)
5. Implement ADR-001 (copy sweep + confirmation popup)
6. Implement ADR-002 (RALPH loop)
7. Final fake-data sweep
